# hugging-voice-protocol

Internal, versioned protocol definitions shared by the GPU service and LiveKit
plugin. This package intentionally has no ML, CUDA, or LiveKit dependency.

Wave 1 adds the strict Pydantic event and audio contracts.
