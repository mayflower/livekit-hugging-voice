apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hugging-voice-models
  annotations:
    livekit-hugging-voice.storage-sizing: adjust-to-the-locked-artifacts-and-storage-class
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 100Gi
